// 类型缩小
function printAll(str: string | string[] | null) {
    if (typeof str === 'object') {
        // for(const s of str){

        // }
    } else if (typeof str === 'string') {
    } else {
    }
}